<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
            <main>
                <ul class="cart-list">
                    <% products.forEach(p=>{ %>
                        <li class="cart-list-items">
                            <p style="font-size: 1.5rem;">
                                <%= p.productId.title %> (<%= p.quantity %>)
                            </p>
                        </li>
                        <% }); %>
                </ul>
                <div class="centered" style="margin-top:2rem;">
                    <h2>Total : ₹ <%= totalSum %>
                    </h2>
                </div>
                <form class="centered" action="/create-checkout-session" method="POST">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <button class="btn bn5" type="submit">Checkout</button>
                </form>
            </main>


            <%- include('../includes/end.ejs') %>